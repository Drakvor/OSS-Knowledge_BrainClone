apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    name: context-manager
    annotations:
        nginx.ingress.kubernetes.io/rewrite-target: /
spec:
    ingressClassName: nginx
    rules:
        - host: oss-knowledge-context-manager.4.230.158.187.nip.io
          http:
              paths:
                  - path: /
                    pathType: Prefix
                    backend:
                        service:
                            name: context-manager
                            port:
                                number: 8005

